{
    "height": 30, "spacing": 4,
    "modules-left": [ "cpu", "custom/gpu", "memory", "custom/quotes", "hyprland/submap" ],
    "modules-center": [ "clock" ], 
    "modules-right": [ "mpris", "custom/cava", "wireplumber", "battery", "tray" ],

    "cpu": { 
        "interval": 2, 
        "format": "CPU {usage}% {avg_frequency}GHz", 
        "max-length": 20 
    },
    "memory": { "interval": 5, "format": "RAM {used:0.1f}G", "max-length": 15 },
    "hyprland/submap": { "format": "{}" },
    
    "clock": { 
        "format": "{:%a %d %I:%M %p}", 
        "format-alt": "{:%Y-%m-%d}",
        "tooltip": false
    },

    "custom/cava": {
        "exec": "stdbuf -o0 cava -p ~/.config/cava/config_waybar | sed -u 's/;//g;s/0/ /g;s/1/▂/g;s/2/▃/g;s/3/▄/g;s/4/▅/g;s/5/▆/g;s/6/▇/g;s/7/█/g;'",
        "format": "{}",
        "tooltip": false
    },

    "custom/gpu": { 
        "exec": "nvidia-smi --query-gpu=utilization.gpu,memory.used --format=csv,noheader,nounits | awk -F', ' '{printf \"GPU %s%% %.1fGiB\", , /1024}'", 
        "format": "{}", 
        "return-type": "", 
        "interval": 2 
    },

    "custom/quotes": { 
        "exec": "python3 ~/.config/waybar/scripts/quotes.py", 
        "interval": 300, 
        "format": "<i>\"{}\"</i>", 
        "tooltip": false 
    },

    "mpris": { "format": " {status_icon} {dynamic}", "interval": 1, "dynamic-len": 40, "status-icons": { "playing": "▶", "paused": "⏸", "stopped": "" }, "ignored-players": ["firefox"] },
    "wireplumber": { "format": "{icon} {volume}%", "format-muted": "", "format-icons": ["", "", ""], "on-click": "pavucontrol" },
    "tray": { "icon-size": 14, "spacing": 10 },
    "battery": { "states": { "good": 95, "warning": 30, "critical": 15 }, "format": "{icon} {capacity}%", "format-charging": " {capacity}%", "format-plugged": " {capacity}%", "format-alt": "{time} {icon}", "format-icons": ["", "", "", "", ""] }
}
